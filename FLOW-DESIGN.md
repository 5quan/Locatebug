# FLOW-DESIGN · 工单预检诊断 Agent 的合理流程设计

> 本文件只讲**技术方案与流程**，不讨论当前代码实现到哪一步。
> 回答一个问题：从"bug 提交触发"到"结论写回工单"再到"结论被验证"，一条**合理的闭环流程**应该长什么样，以及每条边界为什么这么划。

---

## 图 1：端到端主流程（分层 + 副作用边界）

```text
              ┌──────── 触发侧：两种入口，同一语义 ────────┐
              │ ① 工单 webhook（自动）  ② "AI 预检"（一键）│
              └──────────────────┬─────────────────────────┘
                                 │ 原始 payload
                                 ▼
┌──────────────────── 接入层（无状态）─────────────────────────────┐
│ 签名校验 → 适配成 TicketTask → 字段门槛判定                      │
│  必填：ticketId / title / description                            │
│  决定能力：service(日志范围) / commit(是否启用代码工具) /         │
│           occurredAt(时间窗) / traceId(精确检索)                 │
│  字段不全 → 不拒收，记为 material_gaps，让报告走 partial          │
│ 幂等键 idem_key = (ticketId, commit, trigger_reason)             │
└──────────────────────────────┬───────────────────────────────────┘
                               │ 立即 202 + runId  ←── 同步段到此结束
                               ▼
┌──────────────── 生命周期层 Runtime（有状态、唯一权威）────────────┐
│ claim(idem_key)：原子插入 run；已存在 → 返回已受理，不重复跑       │
│ run_started 落库 → 入队（有界队列 + 并发闸 + 背压）                │
│ 持有：预算(迭代/工具/时间) · 取消信号 · 终态守卫                   │
└──────────────────────────────┬───────────────────────────────────┘
                               │ 出队 → 启动一次 run
                               ▼
┌──────────── Agent 循环（编排层：只看得到"工具"，看不到外部系统）──┐
│ 上下文 = 工单字段 + 材料缺口声明 + 预算声明 + 领域知识摘要(按需)   │
│    ↻ 决策 → 材料工具(查日志/搜代码/读代码/扩时间窗/查案例)         │
│              → 证据入池(带 provenance) → 观察事件 → 回到决策      │
│       → 交卷(结构化报告 + 证据闸门) → respond                     │
│       → 终止(预算/无进展/超时/取消)                               │
└──────────────────────────────┬───────────────────────────────────┘
                               │ 事件逐条追加（循环内无任何写副作用）
                               ▼
┌──────── 事实层：事件日志（唯一事实源，append-only）───────────────┐
│ run_started → decision* → tool_* → observation* → usage → 终态×1  │
│ 终态 ∈ { completed(complete|partial) · failed · cancelled }       │
└────┬───────────────────┬────────────────────┬───────────────────┘
     │ 投影A 结构化报告   │ 投影B 进度视图      │ 投影C 审计视图
     ▼                   ▼                    ▼
┌──────────── 投递层（副作用只在这里发生）──────────────────────────┐
│ 备注渲染：结论 + 证据 + 来源 + 置信度 + 缺失材料                   │
│ 目标(端口)：工单备注 API / IM 通知 / 一键复制（无权限时降级）      │
│ 投递状态独立：pending → written | failed（可重试，按(run,target)幂等）│
└──────────────────────────────┬───────────────────────────────────┘
                               ▼
                    ③ 工单现场：开发看到预检报告
                               │
                               ▼
┌──────────── 回程层：反馈（把回环闭上）───────────────────────────┐
│ 确认 / 推翻 / 部分正确 + 修正根因 → feedback 事件（写回同一 run）  │
│ → 投影：接受率 · 命中率 · 失败模式分布 → 评测集 → 提示词/知识迭代  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 图 2：Agent 循环内部（收敛保证在这里）

```text
  上下文（工单 + 材料缺口 + 预算声明）
        │
        ▼
  ┌─ 决策（模型）─────────────────────────────────────────┐
  │ 输入：工单 · 已有观察与证据摘要 · 剩余预算 · 系统规则  │
  └───┬────────────────────────────────────────────────────┘
      │
      ├─ 材料工具 ─▶ 执行（只读 · 限长 · 白名单 · 无 shell）
      │                 ├─ 成功 → 证据入池(source/time/level/excerpt)
      │                 └─ 失败 → **不终止循环**，作为 error 观察交回模型
      │                            （由它决定换条件重查，或走 partial）
      │      ▼
      │   观察事件 ──▶ 回到决策
      │
      ├─ 交卷 ─▶ 结构化报告（schema 强制，不解析自由文本）
      │            ├─ 证据闸门：每条 excerpt 必须命中真实材料
      │            │    命中 → 补真实 provenance
      │            │    未命中 → 标 unverified + 该假设置信度强制降级
      │            ├─ 完整性判定：拿过但没拿到 → 必须 partial + 列缺口
      │            └─ 通过 → respond → 终态 completed
      │
      └─ 终止（外部强制，不由模型自觉）
            · 迭代/工具超预算 → failed(budget_*)
            · 时间超预算（双层：引擎内 + Runtime 外）
            · 无进展（同参数重复查询）→ failed(no_progress)
            · 调用方取消 → cancelled
            · 引擎异常 / 无终态 → Runtime 补 failed（可审计留痕）

  收敛保证：每个分支必然落到唯一终态，不存在"跑着跑着没了"
```

---

## 图 3：两张状态机（最容易被画错的地方）

```text
  诊断 run（一张）                     投递 delivery（另一张）
  ────────────────                    ──────────────────────
  pending ─claim─▶ running            (诞生) ─▶ pending
                    ├─▶ completed       ├─▶ written
                    ├─▶ failed          ├─▶ failed ─retry─▶ written
                    └─▶ cancelled       └─▶ skipped（低置信度/无人确认）
```

**为什么必须分开**：诊断成功但回写失败，不能让 run 变成 `failed`——那会把"结论可能不对"和"结论没送到"混成一件事；分开之后，回写失败**只需重试投递，不用重跑诊断**（省掉一次模型调用）。

---

## 图 4：时间轴（同步段 vs 异步段）

```text
t0     触发（webhook / 点击）
t0+    claim → 202 + runId 返回        ← 同步段结束，人不再等待
t0~t1  后台跑循环：每个事件 → 落库 → SSE 推送（界面若开着，实时看到时间线）
t1     唯一终态落库
t1+    投影报告 → 渲染备注 → 投递 → delivery=written →（可选）通知开发
t2     开发确认/推翻 → feedback 事件 → 评测投影更新 → 影响下一次诊断
```

---

## 八条让流程图"合理"的规则

| # | 规则 | 违反的后果 |
|---|---|---|
| 1 | **副作用只在环外**：循环内只产事件，不做写操作 | 无法重放/重试；重跑会重复回写工单 |
| 2 | **事件是唯一事实源**，报告/进度/审计都是投影 | 出现两个真相；改一处忘一处 |
| 3 | **终态唯一且可预测**，异常路径必须有兜底守卫 | 悬挂 run、永远"进行中"、队列堵死 |
| 4 | **刹车在运行时层，不在 Agent 里**（预算/超时/取消/无进展） | 终止性依赖模型自觉 → 不可预测的成本 |
| 5 | **触发源统一为端口**：自动与一键只是不同适配器 | 两套流程分叉，后面维护两份逻辑 |
| 6 | **诊断终态 ≠ 投递状态**（两张状态机） | 回写失败污染诊断结论；无法单独重试投递 |
| 7 | **闸门在写入路径上**：证据核验必须在结论进事件流之前 | 事后校验等于没校验，脏结论已落库 |
| 8 | **回程必须有**（feedback → 评测 → 知识/提示词） | 只有半圈，系统永远不会变好 |

---

## 六种常见错误画法

| ❌ 错误画法 | ✅ 合理画法 | 为什么 |
|---|---|---|
| 循环里直接调工单 API 回写 | 循环只产事件，回写由投递层消费事件 | 副作用进循环 = 不可重试、不可审计、重跑重复写 |
| 材料查不到就整单 `failed` | 转成"缺口"信息，走 `partial` + 列缺失材料 | "缺什么"本身就是产品价值；失败会丢掉最有用的信息 |
| 让模型自己决定何时收手 | Runtime 强制预算与终止条件 | 成本与延迟要可预测，不能靠模型自觉 |
| 一个状态机管诊断和回写 | 两张独立状态机 | 见规则 6 |
| 报告返回自由文本，事后正则抽字段 | 结构化提交 + 证据闸门 | 无法核验、无法评测、无法稳定渲染 |
| 没有反馈边（工单 → 诊断 → 结束） | 加回程 feedback 事件 | 没有回程就不是回环 |

---

## 一句话总结设计意图

```text
触发 → 生命周期 → 循环 → 事实流 → 投影 → 投递 → 人 → 回程
```

**循环只负责产事件，所有副作用与所有判断（幂等、预算、终态、投递）都在循环外面。**
于是每一段都能独立重试、独立审计、独立替换：换模型不动投递，换工单系统不动循环，回写失败不用重跑诊断。
