# ticket-doctor 项目导读（Onboarding）

> 本文回答五个问题：怎么上手、怎么设计的、怎么思考的、当时怎么选的、怎么向别人介绍。
> 适用版本：阶段 4a 完成（2026-09-06），SDK 基线 @earendil-works/pi-coding-agent@0.84.2。

## 0. 30 秒版

测试提 bug 到开发响应之间有几小时空窗，而开发拿到 bug 后的第一反应已经是"先让 AI 看一眼"。本项目把这一步**前置**：工单提交时自动触发诊断 Agent——拉相关服务的错误日志、**在工单指定的代码版本里检索定位**、做初步根因分析、把**带证据引用**的预检报告写回工单备注。技术栈：TypeScript + Node 原生（无框架），Pi Agent SDK 作为模型层适配器，SDK 版本锁定 0.84.2。

## 0.5 项目口径（唯一权威版：怎么运行 / 什么环境 / 需要什么 / 边界在哪）

**运行形态（三种，同一条链路）**

| 命令 | 形态 | 用途 |
|---|---|---|
| `npm test` | 20 条领域测试 | 验证：不花钱，改完必跑 |
| `npm run doctor:demo` | CLI 一次性，假引擎 | 教学/调试：看事件流与报告 |
| `npm run doctor:real` | CLI 一次性，真机 deepseek | 验证模型行为（单次约 5-8K tokens / 约 1 分钟） |
| `npm run doctor:server`（可加 `DOCTOR_ENGINE=fake`） | HTTP 常驻服务 | 产品形态：接收工单、后台诊断、审计查询 |

**环境要求**：Node ≥ 24（原生跑 TS）；本机 `git`（读代码工具用）；真机运行需项目根 `.env` 提供 `DEEPSEEK_API_KEY`（已 gitignore）。代码工具检索的是**本仓库**（`GitCodeSource` 指向项目根）——接公司代码库时换一个 CodeSource 适配器。

**HTTP 接口**：`POST /api/tickets`（提交工单；body 含 `commit` 字段才启用代码工具）、`GET /api/runs`、`GET /api/runs/:id/events`（完整事件流审计）、`GET /api/runs/:id/progress`（进度投影）、`GET /api/runs/:id/comment`（将写回的备注文本）。

**输入要求（工单质量门槛）**：ticketId/title/description 必填；**带 `service` 才查得准日志；带 `commit` 才启用看代码**（版本在构造时钉死，模型只能看那一版）；带 `occurredAt` 才有时间窗。缺什么，报告会以 partial + missingMaterial 明说。

**能力边界（v1 明确不做）**：一单一诊（改单重触发是 v2 决策）；日志源是本地样例文件（真平台换适配器）；报告到"备注文本"为止，未真写回云效；无鉴权、仅绑本地；假引擎不支持代码工具（确定性教学用）。

**预算与安全（不可协商项）**：日志查询 ≤6 次、代码工具 ≤10 次、超时 120s/180s 双层、同工单幂等、工具白名单（无 bash）、结果限长、事件 append-only 可审计。模型在提示词里**知道自己的预算**（真机治理结论：预算透明化避免被硬刹车掐死在半路）。

## 1. 怎么上手（第一天的 30 分钟）

前置：Node ≥ 24（直接跑 .ts）；真机运行需要项目根 `.env` 里放 `DEEPSEEK_API_KEY`（已被 gitignore）。

按"从便宜到贵"的顺序跑四步，每步都在给下一步打地基：

```bash
npm test          # ① 11 条测试全绿——先读测试再读代码，测试是领域定律不是覆盖率
npm run doctor:demo   # ② 假引擎端到端：看事件流怎么走、报告长什么样（零成本、确定性）
npm run doctor:real   # ③ 真机 deepseek-v4-flash 一次（约 5K tokens / 40 秒）
DOCTOR_ENGINE=fake npm run doctor:server   # ④ HTTP 全链路（curl 见下）
```

第 ④ 步的 curl：

```bash
curl -X POST http://127.0.0.1:7777/api/tickets -H "content-type: application/json" \
  -d '{"ticketId":"BUG-1024","title":"下单接口批量 500","description":"10:02 起 500","service":"checkout-service","occurredAt":1788659520000}'
curl http://127.0.0.1:7777/api/runs/run_BUG-1024/comment   # 应写回工单的备注
curl http://127.0.0.1:7777/api/runs/run_BUG-1024/events    # 完整事件流（审计）
```

读代码顺序（严格按依赖序，每层只依赖下层）：

```
contracts.ts   领域语言：TicketTask/Evidence/DiagnosisReport/Decision/AgentEvent/两个端口
core.ts        领域定律：回放/报告提取/不变量校验/备注渲染（纯函数）
fake-engine.ts 机制：最小循环 + 脚本化"模型" + 预算/空转守卫
log-sources.ts 假日志源（样例文件）
run-log.ts     RunLog：append-only JSONL 落库（header + 事件行）
runtime.ts     Runtime：幂等 claim、事件落库、预算组合、无终态守卫
server.ts      HTTP 触发面（模拟云效 webhook）
pi-adapter.ts  真实引擎（读它之前必须先读 ADAPTER-NOTES.md）
```

读文档顺序：`ADAPTER-NOTES.md`（SDK 事实库，每条断言有出处）→ `ROADMAP.md` 的 ticket-doctor 节（进度与决策）→ `.runs/*.jsonl`（真实事件流标本：BUG-1024/BUG-2001 是 fake 标本，BUG-2002 是真机标本）。

## 2. 它是怎么设计的：一条推导链，不是一张架构图

设计起点是一个句子（不是框架、不是 SDK 特性清单）：

> **给定**一条新提交的 bug 工单，**用户（开发）想要**一份附在备注里的根因分析报告，**当**它在提交后几分钟内自动出现、且每个结论都有证据支撑。

从这句话推出最小循环：`工单 → 上下文 → 模型决策 → 查日志 → 证据 → 报告`。再从循环推出三层切分：**Core**（一次合法的诊断是什么意思）、**Runtime**（一次诊断怎么发生并存活）、**产品面**（人怎么看结果）。SDK、日志平台、云效全部被端口（`DiagnosisEngine`/`LogSource`）关在适配器里。

全部设计服务于三条领域规矩——新增任何代码前先问它加强了哪一条：

1. **说清楚**：证据必须带 provenance（来源+查询条件）；报告假设必须挂证据；模型引用的 excerpt 要经反编造核验，未命中标 `unverified` 并强制降置信度。
2. **认得错**：`complete` 与 `partial` 是类型级区分；partial 必须列 `missingMaterial`；事件流是唯一事实源，状态永远可以从事件回放。
3. **刹得住**：预算、空转守卫、超时全部握在机制侧（引擎+Runtime）；模型只管决策，不管刹车的位置。

## 3. 如何思考（接到新需求时的推理框架）

- **先问推论**：每个新类型/字段/模块，先回答它是哪条需求的推论；连不到任务/决策/证据/终态的就是过早抽象（skill 原话：premature abstraction）。
- **先假后真**：任何外部依赖（模型、日志平台、工单系统）先做假实现把语义钉死成确定性测试，真实现只是端口后面的替换。本项目的 fake/real 走同一套断言。
- **三层放置**：语义进 Core（"五年后还成立吗"测试）、机制进 Runtime（预算/幂等/落库）、供应商细节进适配器（会过期的东西）。
- **断言要落地**：对 SDK 行为的每个假设都要落到源码出处（ADAPTER-NOTES）或真机证据，禁止凭记忆写适配器。
- **变更协议**：升级 SDK = 按三步协议重验映射笔记 + golden 对照；改提示词 = 真机重跑比对（partial 硬规则就是这么修的）。

## 4. 关键选择与理由（决策记录）

| 选择 | 放弃了什么 | 决定性理由 |
|---|---|---|
| Pi SDK 做模型层 | 继续手写 llm.ts 协议代码 | provider/重试/工具调用/事件流现成；手写协议不产生业务价值 |
| 事件流是脊柱 | 函数直接返回报告 | 审计、回放、恢复、进度全部消费事件流；报告只是事件流的投影 |
| `submit_report` 工具交报告 | 自由文本+JSON 解析 | TypeBox schema 在 SDK 层强制报告形状，畸形报告自动被打回重试 |
| JSONL RunLog（一 run 一文件） | 数据库 | 单机文件够用、天然 append-only、文本工具可直接查；端口在，将来可换 |
| `wx` 独占创建做幂等 | 先查重再创建 | 并发/重启下"查了再建"有竞态窗口；claim 必须原子 |
| 假实现先行 | 直接接真 SDK | 领域语义必须确定性可断言；真模型贵、慢、有方差 |
| partial 一等公民 | 查不到日志就 run_failed | 日志平台挂了也要在工单上留下"缺什么"——这是产品价值所在 |
| `noTools:"builtin"` | 默认工具集 | 消费不可信工单内容的服务绝不给 bash（skill Security defaults） |

## 5. 如何向别人介绍

**30 秒电梯稿**：
> 测试提了 bug，开发几小时后才看到，然后第一件事是让 AI 帮忙看日志。我们把这一步前置到工单提交的那一刻：Agent 自动拉错误日志、做根因分析，把带证据引用的预检报告直接写回工单备注。开发接手时看到的不是"xx 接口报错"，而是日志片段、根因假设、建议排查动作都摆好的问题单。核心卖点不是"用了 AI"，是**每个结论可审计**——证据从哪查的、为什么这么判、缺了什么材料，全部在事件流里。

**3 分钟版结构**（痛点 → 方案 → 证据 → 边界）：
1. 痛点：提 bug 到开发响应之间的空窗 + "让 AI 先看一遍"已是习惯动作（引出"前置"）。
2. 方案：一条最小循环 + 三层架构（Core/Runtime/适配器），现场跑 `doctor:real` 看 40 秒出报告。
3. 证据：11 条领域测试 + 真机三次运行 partial 判定一致 + 0 条未核实证据 + 幂等/留痕/超时全有测试。
4. 边界：诚实说 v1 边界（样例日志源、未真写回云效、无鉴权）——边界清楚反而加分。

**按听众调整**：对开发同事重点演示报告本身（"你接手时证据都摆好了"）；对技术负责人重点讲可靠性设计（原子幂等、append-only 审计、预算刹车）和成本（一单约 5K tokens）；对面试官准备三个可深挖点——第一性原理推导方法、事件溯源（状态=f(事件)）、证据反编造核验。

**常见问题 Q&A**：
- *和 AIOps 平台什么区别？* 不替代监控告警，只填"工单到开发之间"的空窗；差异点是证据可审计、以工单为载体。
- *误报怎么办？* 报告自我定位"预检参考，非最终结论"；每条证据可回溯；unverified 证据强制低置信度；二期可加写回前人审闸门。
- *成本？* 单次约 5K input + 300 output tokens；空转/超时/迭代全有预算闸，最坏情况有上界。
- *安全？* 不给 bash、日志只读、密钥不进事件流（gitignore + 环境变量）、服务仅绑本地，接云效时再加鉴权。

## 6. 当前边界与下一步

- **v1 边界**：一单一诊（改单重触发是 v2 决策）；日志源是本地样例文件（真平台换适配器）；代码源指向本仓库（公司代码库换 CodeSource 适配器，按工单 commit 钉版本）；报告到"备注文本"为止，未真写回云效；服务无鉴权仅本地。
- **阶段 4 剩余**：云效 webhook payload → TicketTask 适配器、真实日志源（SLS/ELK）适配器、备注写回云效 API 或钉钉通知。
- **阶段 5**：golden runs 回归语料（钉住事件序列，防 SDK 升级悄悄改语义）、提示词治理、按 skill 退出清单做产品验收。
