# ticket-doctor：用前后端架构重读 + 重新设计

> 目的：回答两个问题——"这个 demo 到底长什么样"（用你熟悉的前后端词汇）和"该重新设计哪里"。
> 结论先给：**后端骨架不用重写（它是对的），真正缺的是"前端那一半"和"反馈那一半"。**

---

## 0. 为什么你会觉得"看不懂"（不是代码烂，是四个具体缺口）

| # | 现象 | 证据 | 后果 |
|---|---|---|---|
| 1 | **文档和代码脱节** | README.md / ROADMAP.md 前半部分描述的是 `src/agent.ts`、`llm.ts`、`server.ts`、`session-store.ts`、`public/index.html` —— 这些文件在 `git status` 里全是 `D`（已删除）；`src/` 现在只剩 `ticket-doctor/` | 你按 README 的"目录"去找文件，一个都找不到，第一反应就是"我看不懂" |
| 2 | **命名不是前后端词汇** | `contracts.ts` / `core.ts` / `runtime.ts` / `*-sources.ts` / `pi-adapter.ts` | 这是六边形架构（端口与适配器）的词汇，不是 Controller/Service/DAO/前端那套，你脑中没有对应槽位 |
| 3 | **只有后端，没有前端** | 全仓库没有 `public/`、没有 HTML、没有 SSE，只有 `GET /api/runs/:id/comment` 返回纯文本 | 你"看"的是一个没有界面的后端服务，当然感觉不到产品形态，只能 curl |
| 4 | **工作区没提交** | 只有 1 个 ticket-doctor 提交（`9915e19 工单助手第一阶段`），20+ 个 ticket-doctor 文件是 untracked，旧项目文件是未提交的删除 | 状态不可回退，也没法用 `git log` 读演进史，进一步加重"乱"的感觉 |

顺手记两条待修的小口径不一致（P0 文档归位时一起处理）：
- 测试条数：`ONBOARDING.md` 写 "11 条"，`ROADMAP.md` 写 "20/20"，`package.json` 的 test 脚本跑 4 个测试文件 —— 三处需要统一成一个口径。
- `README.md` 说 `src/repl.ts` 是废弃入口 —— 该文件已不存在。

---

## 1. 用前后端架构重读：一张对照表

把你熟悉的那套分层，一一映射到这个项目上（左边是你会写的词，右边是这里的落点）：

| 前后端概念 | 本项目对应物 | 文件 | 一句话说明 |
|---|---|---|---|
| **路由 / Controller** | HTTP 接入、DTO 校验、状态码 | `server.ts` | `POST /api/tickets` 202 立即受理；`validateTicket()` 就是网关参数校验（含 ticketId 非法字符拦截、1MB body 限制） |
| **Service / 应用服务** | `TicketDoctorRuntime` | `runtime.ts` | 幂等 claim、超时预算、后台执行、异常与"无终态"兜底落库 —— 业务用例的编排者，不碰 HTTP 也不碰 SDK |
| **Domain / 领域契约** | `TicketTask`、`Evidence`、`DiagnosisReport`、`Decision`、`AgentEvent` | `contracts.ts` | 项目里唯一"不 import 任何 SDK"的文件；它其实是**需求文档的机器可读版本** |
| **Domain Service / 纯逻辑** | 回放、不变量校验、报告提取、备注渲染 | `core.ts` | 全是纯函数，不碰网络/文件/当前时间 —— 测试和回放都建立在它上面 |
| **Repository / DAO** | `RunLog`（append-only JSONL） | `run-log.ts` | 一 run 一文件、首行 header、事件行追加、按 run 串行写队列 |
| **RPC Client / 第三方 SDK 封装** | `LogSource` / `CodeSource` / `DiagnosisEngine` 三端口的实现 | `log-sources.ts`、`code-sources.ts`、`pi-adapter.ts`、`fake-engine.ts` | SDK 类型与事件**不出适配器边界**（`pi-adapter.ts` 开头那条铁律） |
| **DTO / ViewModel（BFF 层）** | `RunProgress`、`renderTicketComment()` 的输出 | `progress.ts`、`core.ts` | 后端返回给"展示端"的读模型，不是原始事件 |
| **前端页面** | ❌ **不存在** | — | 最大的缺口 |
| **实时通道（SSE/WS）** | ❌ **不存在** | — | 现在只能轮询 `GET /api/runs/:id/progress` |

### 一次工单的完整时序（横向看数据怎么流）

```text
云效/钉钉 webhook
   │  POST /api/tickets   {ticketId,title,description,service,occurredAt,commit}
   ▼
server.ts        校验 DTO → runtime.submit(task)
   ▼
runtime.ts       幂等 claim：.runs/run_<ticketId>.jsonl 用 wx 独占创建
   │               成功 → 202 立即返回（不等诊断结束）
   ▼  后台执行（不阻塞 HTTP）
engine.run(task, signal)   ← 端口；fake 引擎 或 Pi SDK 适配器
   │  异步吐事件：decision_made → tool_started → tool_completed → observation_added → ...
   ▼
runtime.ts       每个事件原样落库 appendEvent()；异常/无终态 → 补 run_failed
   ▼
run-log.ts       .runs/run_<id>.jsonl（唯一事实源，只追加）
   │
   ├─→ progress.ts  projectProgress(events)   → GET /api/runs/:id/progress   （读模型 A：进度）
   ├─→ core.ts      renderTicketComment(...)  → GET /api/runs/:id/comment    （读模型 B：备注）
   └─→ ❌ 缺失：写回云效备注适配器、前端控制台、SSE 实时推送
```

**这张图就是全部答案**：左边（接入、领域、存储、适配器）已经完整，右边（呈现、实时、回写、反馈）整条线还没写。

---

## 2. 用你熟悉的后端模式给它"翻译"一遍

一旦翻译成熟悉的模式，这个项目就不再神秘了：

| 你熟悉的模式 | 这里的体现 | 为什么这么设计（面试可讲的理由） |
|---|---|---|
| **Event Sourcing + CQRS** | `.runs/*.jsonl` 是写模型（唯一真源、只追加）；`replayAgentState()` / `projectProgress()` / `renderTicketComment()` 是三个**读模型投影** | 删掉 `progress.ts` 不会丢任何信息，随时能重算。审计、恢复、进度、报告全都在消费同一条事件流 |
| **唯一索引 / 分布式锁做幂等** | `open(file, "wx")` 独占创建 = `INSERT ... ON CONFLICT DO NOTHING` | "先查有没有再创建"有竞态窗口；claim 必须原子。并发提交、进程重启后重复提交都只有一次能成功 |
| **事务不许悬挂** | Runtime 的"无终态守卫"：引擎正常结束却没给终态 → 自动补一条可审计的 `run_failed` | 任何一次 run 都必须可预测地终止，否则排队/审计/告警全都悬空 |
| **网关限流 / 配额** | 预算：日志查询≤6、代码工具≤10、超时 120s（引擎）/180s（Runtime） | 而且**把配额告诉调用方**（预算数字写进系统提示词）—— 对应真机踩坑"不告诉模型预算，它会把次数烧光被硬刹车掐死" |
| **依赖倒置 / 防腐层** | 日志平台、Git 仓库、LLM SDK 全部藏在端口后面 | 换 SLS/ELK 只写一个 `LogSource` 实现；换模型供应商只写一个 `DiagnosisEngine` 实现，Core 一行不改 |
| **不可信输入处理** | 日志/代码内容是外部输入：条数限长（20 条）、单行限长（2000 字符）、证据截断（200 字符） | 对应前端侧的同类问题：这些 excerpt 渲染到页面上必须转义（XSS 面），这是前后端的安全边界 |
| **fail-fast vs 业务错误** | `FileLogSource` 遇到日志文件不存在 → **抛错**而不是返回空数组 | "服务不存在"和"服务没有错误日志"是两个完全不同的诊断结论 |

---

## 3. 重新设计：缺的是"前端那一半"和"反馈那一半"

### 3.1 产品缺口清单（现在后端有能力，但用户看不到）

| 后端已有 | 用户（测试/开发）看不到什么 | 影响 |
|---|---|---|
| run 已经受理并在后台跑 | 诊断"正在进行中"不可见，没有 SSE，只能自己轮询 `/progress` | 提交后一片安静，用户以为没反应 —— AI 介入的价值必须"可见"才有说服力 |
| `/comment` 能渲染出完整报告文本 | 没写回云效/钉钉，也没有任何卡片渲染 | 开发不会主动来 curl，价值无法到达工单现场 |
| 证据核验、unverified 降级、partial 判定 | 这些"可信度"信号没有可视化出口 | 你最强的卖点（可审计、不编造）用户完全感知不到 |
| 事件流里什么都有 | 没有任何反馈入口（开发确认/推翻根因） | **无法评测、无法迭代提示词**；你简历里写的"知识命中与结论修正、反馈回路"目前没有落点 |
| 超时/预算/取消机制齐全 | 没有取消按钮、没有重试 | run 卡住只能干等 180s |
| 审计用的 `/events` 接口 | 没有回放页面 | 审计能力只对 curl 用户存在 |

### 3.2 目标形态：三张视图（按 80/20 排序）

| 视图 | 给谁 | 内容 | 实现成本 |
|---|---|---|---|
| **A. 工单内嵌卡片** | 测试 + 开发 | 结论 + 置信度 + 建议下一步，证据默认折叠 | 最低（就是 `/comment` 的富文本版，写回云效即可） |
| **B. 诊断控制台** | 开发 / 你自己 | run 列表 + 实时时间线（决策→工具→观察→证据）+ 阶段进度 + 预算消耗 + 取消按钮 | 中（需要一个前端 + 一条 SSE） |
| **C. 审计回放页** | 技术负责人 / 复盘 | 完整事件流、每条证据的来源与核验结果（unverified 高亮） | 中（前端读 `/events` 即可，后端不用改） |

### 3.3 后端只加两条线（骨架不要动）

1. **`GET /api/runs/:id/stream`（SSE）**
   - 语义：先回放 RunLog 里已有的历史事件，然后 tail 新增事件；`id:` 字段用事件的 `sequence`。
   - 这样**断线重连可以直接用 `Last-Event-ID` 续传**，而且不引入任何新状态源（进度依然是 `f(事件)`）。
   - 为什么 SSE 不用 WebSocket：需求是单向推送 + 自动重连 + 复用 HTTP 基础设施，WS 的双向能力在这里没有用途（这个"为什么不用 WS"就是一道技术选型题）。
2. **`POST /api/runs/:id/feedback`（反馈闭环）**
   - body：`{ verdict: "confirmed" | "rejected" | "partially_correct", correctedCause?, comment?, actor }`
   - 落库方式：**作为一条 `feedback` 事件写进同一个 run 文件**（保持"唯一真源、只追加"不变），而不是另开一张表。
   - 收益：立刻获得评测语料（命中/推翻统计）、提示词迭代依据、以及"经验沉淀"的落点。
3. （可选）`POST /api/runs/:id/cancel`：调 `AbortController`，事件流落一条 `run_cancelled`。

### 3.4 契约先行：加一份"前后端共享契约"

现在的 `progress.ts` 里的 `RunProgress` 其实就是**前端要的 DTO**，只是藏在后端目录里。重构建议：

```text
src/ticket-doctor/contracts.ts        # 领域契约（保留）
src/ticket-doctor/view-contracts.ts   # 新增：RunProgress（从 progress.ts 移出）+ StreamEvent（SSE 帧）
```

前端只 `import type`，不复制任何投影逻辑 —— 前端永远拿不到"计算权"，只做渲染。这条纪律能防止前端的展示逻辑慢慢长出第二套业务规则。

### 3.5 前端选型与理由（这几条就是简历/面试要的"技术选型"）

| 选型 | 放弃的选项 | 决定性理由 |
|---|---|---|
| React + Vite + zustand（单页控制台） | 用现有 `Node 原生 http` 直接吐 HTML | 事件流 → store 的单向数据流与事件溯源天然同构；但**先做只读版**，不要一步到位 |
| SSE | WebSocket | 单向推送 + 自动重连 + 可复用 HTTP 层；`Last-Event-ID` 直接映射到 `sequence` |
| 只读渲染 + 显式转义 | 直接 `innerHTML` 拼 excerpt | 日志/代码内容是**不可信输入**（这条在后端已经体现为限长，前端必须体现为转义） |
| 暂不引数据库 | Postgres / SQLite | JSONL 对单机 v1 够用，且天然 append-only、文本工具可直接查；真要换，只改 `RunLog` 的一个实现 —— 这就是端口设计的回报 |
| 不引 MCP / RAG | 上向量库 | 材料源已经由 `LogSource`/`CodeSource` 端口固定，先证明诊断质量再谈检索升级（ROADMAP 已明确不做） |

### 3.6 关于"把 Go 用上"：两条路线

**路线 A（✅ 已选定）：双语言分工 —— Go 做接入与调度，TS 留 Agent 引擎**

| 层 | 语言 | 内容 | 理由 |
|---|---|---|---|
| 接入 / 调度 | **Go** | 云效 webhook 接收与验签、限流、任务队列与并发闸、SLS/ELK 日志客户端、备注写回 / 钉钉通知、多租户与审计 | 高并发 IO、长驻服务、外部系统集成是 Go 的强项；goroutine + `context.Context` 天然对应"一次 run 一个生命周期"+ 取消穿透 |
| Agent 编排 | TypeScript | `contracts` / `core` / `runtime` / `pi-adapter` | Pi SDK、TypeBox、工具调用生态都在 TS 侧；重写等于自己实现 provider/重试/工具调用治理 |
| 边界 | **事件契约** | Go 侧只认 `TicketTask` 与 `AgentEvent`（JSON over HTTP/队列） | 两种语言靠契约解耦，谁都不许直接连对方的内部 |

面试版说法："把高并发集成与调度交给 Go，把模型编排与工具语义留在 TS，边界由事件契约固定，两侧都可独立替换。"

**路线 B：全 Go 重写**

- 迁移难度：`contracts`（纯类型）、`core`（纯函数）、`runtime`（并发控制）、`run-log`（文件 IO）、`server`（HTTP）**都是 1:1 可迁移的**，Go 里分别是 struct/interface、纯函数、`context.Context` + `errgroup`、`os.OpenFile(O_EXCL)`、`net/http`。
- 真正要重写的是 `pi-adapter.ts`：失去 SDK 的 provider 管理、自动重试、工具调用循环与 schema 校验，得自己写 agent loop（这恰好是你 README 里"亲手实现最小循环"的原始目标）。
- 收益：单一语言、单二进制部署、"从零实现 Agent 循环+工具调用"在简历上说服力更强；代价：短期多花 2~3 天，且要自己补回 SDK 已经治理好的错误归一化。
- 折中：**保留 TS 作为"参考实现"**，Go 版本只做 `contracts` + `core` + `runtime` + `run-log` + `server`（也就是不接真模型，用假引擎跑通），照样能讲清事件溯源与幂等。

### 3.7 分阶段落地（每步都能独立交付、独立写进简历）

| 阶段 | 做什么 | 改哪些文件 | 简历价值 |
|---|---|---|---|
| **P0** | 文档归位 + 提交当前代码 | 重写 `README.md`（对齐 ticket-doctor，删掉已删除项目的章节）、统一测试条数口径、`git add` 提交 | 让项目"可被人读懂"，这是 0 号工程能力 |
| **P1** | SSE + 只读控制台 | `server.ts` 加 `/stream`；新增 `view-contracts.ts`；前端一页（run 列表 + 时间线 + 报告卡片） | "实时可见"+"前后端契约先行" |
| **P2** | 反馈闭环 | `contracts.ts` 加 `feedback` 事件类型；`server.ts` 加 `POST /feedback`；`progress.ts` 加命中率统计 | 评测语料 + 提示词迭代依据（也是你简历里"反馈回路"的真实落点） |
| **P3** | 真实写回 | 新增 `ticket-reporters.ts`（`TicketReporter` 端口 + 云效/钉钉实现） | "把 AI 结论送回业务现场"的最后一公里 |
| **P4** | golden runs + 提示词版本化 | 把 `.runs/*.jsonl` 当回归语料；run header 记录 `promptVersion`/`modelId` | SDK 升级与提示词改动的防回归（ROADMAP 阶段 5 已列） |

---

## 4. 明确**不要**动的部分

- 端口与适配器分层（`contracts` → `core` → `runtime` → 适配器）—— 这是这个项目最值钱的设计，重写会把它丢掉。
- 事件流唯一事实源 + append-only。
- fake / real 双引擎（"先假后真"）。
- 预算与终止机制、`partial` 一等公民、证据必须带 provenance。

**一句话总结**：这个 demo 的问题从来不是"后端写错了"，而是"只有后端 + 文档过期 + 没有界面"。补上 SSE、一个只读控制台和反馈入口，它就从"能跑的 demo"变成"能演示的产品"。

---

## 5. 收敛后的方案（三个约束已定）

已确认的三个前提：

1. **目标 = 冲简历/面试（AI 应用 / Agent 方向）**
2. **材料源 = 代码能看到，日志平台没有权限**
3. **触发方式 = 先做一键触发**

这三条带来了三个结构性变化，其中第 1 条推翻了本文档第 3.6 节"Go 双语言"的优先级。

### 5.1 Go 降级（从"主线"降为"可选加分项"）

理由是本文档 3.6 节自己写过的分叉规则：**目标是 AI 应用/Agent 岗 → TS 全栈更值**。Agent 岗面试官几乎不会问 Go，但一定会问"可信度怎么保证""评测怎么做""为什么不用多 Agent"——这三件事的投入产出比远高于接入层换语言。

- **降级后的正确姿势**：不删概念，只留 **200 行以内的最小接入层**（webhook 接收 + 原子幂等 claim + 有界队列），定位是"证明我懂服务端边界"，而不是"我用 Go 做了接入层"。
- **或者更省**：完全不写 Go，只在决策记录里保留"为什么接入层与引擎分层、为什么边界是事件契约"——面试时讲设计，不指代码，也站得住（因为这是设计判断，不是能力声明）。
- **禁止**：Go 侧做任何业务语义判断（一旦 Go 也开始判断"这段日志算不算异常"，语义就分叉了）。

### 5.2 日志没权限 → 不是降级，是产品定位变了

关键洞察：**测试同学提 bug 时手边本来就有日志**（控制台报错、日志文件、堆栈文本）。所以"日志以粘贴/文件形式进入"不是妥协，而是**最贴近现实的输入形态**——比"我去日志平台查"更真实。而他"代码能看到"，意味着**代码检索成为主材料源**。

新的定位：**日志在手，诊断我有** —— Agent 的任务从"跨平台检索日志"变成"读懂手上的日志 + 在指定版本代码里定位 + 给出带证据的根因预检"。

同时，"没权限"这个约束本身就是**端口设计的真实叙事来源**，面试讲起来极有说服力：

> "我一开始想直接查日志平台，但没有权限、而且工单里本来就常带日志片段，所以我把材料源抽象成端口，先用文件与粘贴实现，平台适配器后置——换 SLS 只改一个实现，Core 和引擎一行不动。"

### 5.3 一键触发 → 前端从"最后做"升为"MVP 核心"

- 一键 = 一个按钮 = **必须有界面**。所以 SSE 与单页界面从原 P1 提前为 MVP 主干。
- 同时它绕开了企业系统集成权限："一键触发 + 一键复制报告到剪贴板"可以**零云效 API 权限**完成端到端闭环，`TicketReporter` 端口保留但不阻塞演示。
- 对简历的额外收益：**有界面、可录屏、可截图**——Agent 岗面试官很吃"能当场演示的东西"。

### 5.4 新形态（把后台服务改造成诊断工作台）

```text
[单页工具]
  ├─ 左：粘贴工单文本（标题/描述）
  ├─ 左：粘贴日志文本 / 选择日志文件（多形态归一化）
  ├─ 左：选择代码仓库路径 + 版本（commit）
  ▼  点"开始诊断"
POST /api/diagnose → 202 → RunLog 落一条 run
  ▼  SSE /api/runs/:id/stream
  ├─ 实时时间线：决策 → 工具调用 → 观察到证据（带进度与预算消耗）
  ├─ 取消按钮（AbortSignal 穿透）
  ▼
报告卡片：结论 + 置信度 + 证据（可复现的查询条件 / 文件#行号）+ 缺失材料
  └─ [一键复制到工单备注]  [确认 / 推翻根因 → feedback 事件]
```

一句话：**从"webhook 后台服务"变成"你坐在前面等的诊断工作台"**。

### 5.5 四个新增技术点（都是 Agent 岗会被追问的加分项）

| 技术点 | 内容 | 面试价值 |
|---|---|---|
| **非结构化工单 → 结构化任务** | 从标题/描述里抽取 `service`、`traceId`、异常类名、接口路径、发生时间；先规则后 LLM，**抽取结果在界面上可人工修正** | "如何把非结构化输入变成结构化任务"是 Agent 工程的核心问题之一；也替代了"要求测试同学填字段"这个不现实的流程改造 |
| **多形态日志归一化** | 粘贴文本 / 文件 / 平台 API 三种实现同一 `LogSource` 端口；解析 Java 堆栈、JSON 日志、Nginx 日志 → 统一 `LogEntry` | 不可信输入解析 + 适配器模式，可单测、可讲 |
| **代码检索升为主材料源** | 版本钉死的 `git grep` 定位 → 读取上下文 → 证据带"文件#行号" provenance | 已有实现，现在从"延伸能力"变成"主干能力" |
| **评测与反馈闭环** | `feedback` 事件（确认/推翻/部分正确 + 修正根因）+ 命中率统计 + `promptVersion` 记录 | "我怎么知道它有没有变好"——Agent 岗最缺也最看重的一环，目前项目里完全没有 |

### 5.6 执行顺序（约 5~6 天，每步都能独立演示）

| 步 | 做什么 | 工时 | 可演示的产出 |
|---|---|---|---|
| 1 | 定三个契约：输入抽取契约、报告契约、诊断键（建议 `(ticketId, commit)` 或"一轮一次 run"） | 0.5 天 | 一页契约清单 |
| 2 | 日志导入与归一化（粘贴/文件），替换或并存于样例文件源 | 1 天 | 贴一段堆栈就能解析成 LogEntry |
| 3 | 工单文本 → `TicketTask` 抽取（规则 + LLM）+ 界面人工修正 | 1 天 | 贴标题描述自动填出 service/traceId |
| 4 | SSE + 单页界面（粘贴 → 实时时间线 → 报告卡片 → 一键复制） | 1.5 天 | 可录屏的完整演示 |
| 5 | `feedback` 事件 + 命中统计接口 | 1 天 | "AI 结论被确认率" |
| 6 | 代码源指向真实仓库路径（可配置）+ 跑通一条真实案例 | 0.5 天 | 真实 bug 的报告 |
| 7（可选） | Go 最小接入层 200 行，或只写决策记录 | 0.5 天 | 边界设计的谈资 |

### 5.7 面试叙事（三条可深挖 + 一次演示）

1. **设计判断力**："日志平台没权限，我没有硬凑，而是把材料源抽象成端口，用最真实的输入形态（工单里的日志）先做——换平台只改一个实现。"
2. **可信度**："AI 写进工单的不是结论，是一份可审计的证据线索：每条证据能复现，拿不到材料就说缺什么，绝不拿推断冒充事实。"
3. **评测**："我记录了结论被确认/推翻的比例，用它迭代提示词和知识；多 Agent 我没做，因为收益必须由评测证明，而不是由 Agent 数量判断。"
4. **演示**：现场贴一段日志 → 40 秒出带证据的报告（`npm run doctor:real` 或界面版）。
